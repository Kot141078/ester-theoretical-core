# Independent reproduction report

```text
Release tag:
Archive SHA-256:
Operating system:
Shell and version:
Python and dependency versions:
Commands executed:
Exit codes:
Expected and observed counts:
Manifest verification:
Changed files after execution:
Network used:
Result: PASS / BLOCKED / PACKAGE FAILURE
```

Attach command logs and hashes. A report-file change is not automatically a
frozen-source change; state the compared surfaces explicitly.
